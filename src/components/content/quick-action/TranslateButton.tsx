import language from '../../../lib/language'
import { QuickActionButton } from './QuickActionButton'

export const TranslateButton = () => (
  <QuickActionButton onClick={() => {}}>{language.en.buttons.translate}</QuickActionButton>
)
